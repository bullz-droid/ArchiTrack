{
  "name": "architrack",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "start": "npm run dev",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "build": "cd client && npm install --include=dev && npm run build && cd .. && mkdir -p public && cp -rv client/dist/* public/",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "supabase": "^2.84.4"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.100.1"
  }
}
